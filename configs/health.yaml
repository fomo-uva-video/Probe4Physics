defaults:
  - override hydra/hydra_logging: disabled
  - override hydra/job_logging: disabled

synthetic_forward: false
device: cpu

hydra:
  run:
    dir: .
  output_subdir: null
  job:
    chdir: false
